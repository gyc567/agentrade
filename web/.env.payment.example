# Crossmint Payment Configuration
# Copy this to .env.local and replace with your actual keys

# Crossmint Client-side API Key (public, can be exposed)
# Get from: https://console.crossmint.com/api-keys (select "Client-side keys")
NEXT_PUBLIC_CROSSMINT_CLIENT_API_KEY=ck_staging_your_api_key_here

# (Optional) Crossmint Server-side API Key (for webhook verification, backend only)
# Get from: https://console.crossmint.com/api-keys (select "Server-side keys")
# Only needed if implementing webhook signature verification
CROSSMINT_WEBHOOK_SECRET=your_webhook_secret_here

# Environment
# Set to "staging" for testing, "production" for live
VITE_PAYMENT_ENV=staging
